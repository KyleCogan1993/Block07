// Created the four consts of the greeting string, and numbers for each of the combination numbers

const greet = "You have received this message because you have been chosen to open an important value.  Here is the secret combination: ";

//set the first to 10 using the + operator
const first = 8+2;

//set the second to 40 using the * operator
const second = 2*20;

//set the third to 39 using the - operator
const third = 40-1;

//creates the popup alert of the greeting with the three constants concat'd
alert(greet + first + "-" + second + "-" + third);
